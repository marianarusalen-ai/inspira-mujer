---
import PageLayout from '@/layouts/PageLayout.astro';
import { resolveSeo, buildCanonical } from '@/config';

const seo = resolveSeo({
  title: 'Eventos',
  description: 'Talleres, encuentros y eventos de Inspira Mujer.',
  canonical: buildCanonical('/eventos'),
});
---

<PageLayout seo={seo}>
  <h1>Eventos</h1>
</PageLayout>
